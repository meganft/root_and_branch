<h1 class="indexheader">All Items</h1>

<div class="row">
<% @items.each do |item| %>
  <div class="col-md-4"><%= link_to image_tag(item.image, class: "img-thumbnail"), item_path(item) %><br>
  <%= item.title %>; <%= number_to_currency(item.price) %><br></div>
<% end %>
</div>
