<div class="container">
  <div class="displaypage">
  <h2>All Items</h2>
  <% @items.each do |item| %>
      <li><%= item.title %> <%= link_to "Edit", edit_admin_item_path(item) %></li>
    <% end %>
  </div>
</div>
